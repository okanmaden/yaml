apiVersion: v1
kind: Pod
metadata:
  name: ornekpod # pod ismi
spec:
  containers:
  - name: nginx # container ismi
    image: nginx:latest #containerin oluşacağı imaj
    ports:
    - containerPort: 80 #containera dışarıdan erişim sağlanması için port tanımı
