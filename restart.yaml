apiVersion: v1
kind: Pod
metadata:
  name: imageerrorpod
spec:
  restartPolicy: Always
  containers:
  - name: imageerrorcontainer
    image: naberx
  containers:
  - name: nginx
    image: nginx:latest
